GitHub notes

git init  % starts git in the current directory

git add README.md % stages README.md before comit

git commit -m "name of commit"

git status % check status of commit

git remote add origin https://github.com/elandahl/dynamical-diffraction.git

git push -u origin master


git checkout -b compatibility_branch  % starts new branch called compatibility_branch

git branch  % view branches, see which is current

git checkout master % go back to master branch

git merge compatibility_master % merge compatibility_master into 